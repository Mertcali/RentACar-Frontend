import { CarFilterPipePipe } from './car-filter-pipe.pipe';

describe('CarFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CarFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
