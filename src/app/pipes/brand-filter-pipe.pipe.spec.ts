import { BrandFilterPipePipe } from './brand-filter-pipe.pipe';

describe('BrandFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BrandFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
